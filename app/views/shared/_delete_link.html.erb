<% if !object.nil? && !current_user.nil? %>
 <%= link_to "delete", micropost_path(object), method: :delete, confirm: 'Are you sure ?', title: object.content if  object.user_id==current_user.id %>
 <% end %>